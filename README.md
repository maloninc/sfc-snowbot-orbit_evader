# Evasive Bot (CROBOTS-inspired)

A lightweight example bot that use the public SnowBot API.
Keeps moving in a gentle orbit (turn + short move) and only throws when a target is almost straight ahead and within medium range.
